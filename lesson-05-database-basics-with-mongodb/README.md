## Homework Project

Simple Blog Platform

Students should design MongoDB collections for:

```
users
posts
comments
likes
categories
```

For each collection, students should write:

```
1. Collection name
2. Fields inside the collection
3. Example document
4. Which fields are ObjectId references
5. Which indexes are needed
6. Which data can be embedded
7. Which data should be stored separately
```